VITE_API_URL="http://your_api_url:3000"

# Health Check Configuration
# Define se o build deve falhar quando o backend não estiver disponível
# Valores: true (padrão) | false
FAIL_ON_HEALTH_CHECK=true